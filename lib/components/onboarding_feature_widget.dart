import '/flutter_flow/flutter_flow_theme.dart';
import '/flutter_flow/flutter_flow_util.dart';
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';
import 'package:provider/provider.dart';
import 'onboarding_feature_model.dart';
export 'onboarding_feature_model.dart';

class OnboardingFeatureWidget extends StatefulWidget {
  const OnboardingFeatureWidget({super.key});

  @override
  State<OnboardingFeatureWidget> createState() =>
      _OnboardingFeatureWidgetState();
}

class _OnboardingFeatureWidgetState extends State<OnboardingFeatureWidget> {
  late OnboardingFeatureModel _model;

  @override
  void setState(VoidCallback callback) {
    super.setState(callback);
    _model.onUpdate();
  }

  @override
  void initState() {
    super.initState();
    _model = createModel(context, () => OnboardingFeatureModel());

    WidgetsBinding.instance.addPostFrameCallback((_) => safeSetState(() {}));
  }

  @override
  void dispose() {
    _model.maybeDispose();

    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      decoration: BoxDecoration(
        color: FlutterFlowTheme.of(context).secondaryBackground,
        borderRadius: BorderRadius.circular(12.0),
      ),
    );
  }
}
